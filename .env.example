# Antigravity Phone Connect Configuration

# Your password for the mobile interface (optional if using Tailscale only)
# Used to secure your mobile dashboard
APP_PASSWORD=your-app-password

# AI Provider API Key
# Replace 'xxx' with your provider name (e.g., GROQ_API_KEY, GEMINI_API_KEY)
# xxx_API_KEY=your-xxx-key

# Server Port
PORT=3000

# Networking: Tailscale handles remote access.
# Just ensure Tailscale is running on both this machine and your phone.
# Access via http://<tailscale-hostname>:3000
